#include <cctype>
#include "sys.h"

int main() {

    /* int grade = 0;
    std::string lettergrade;

    std::cout << "请输入分数" << std::endl;
    while ( std::cin >> grade) {
        ScoresToLevel(grade,&lettergrade);
    } */
    unsigned a_cnt,e_cnt,i_cnt,o_cnt,u_cnt = 0;
    std::string text;

    while (std::cin >> text) {
        switch (text) {
            case "a":

        }
    }


}